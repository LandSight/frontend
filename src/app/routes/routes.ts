import { reatomRoute } from '@reatom/core';

export const loginRoute = reatomRoute('login');
export const registerRoute = reatomRoute('register');
export const mapRoute = reatomRoute('map');
export const analysesRoute = reatomRoute('analyses');
export const reportRoute = reatomRoute('reports/:id');
